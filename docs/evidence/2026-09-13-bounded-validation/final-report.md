# 有界并行验收最终报告

2026-09-13。固定B01–B16已全部取得结论：**9项通过、6项外部阻塞、1项失败**。
本次验收目标完成，不能据此宣称全部能力通过或直接批准全量发布。

逐项条件、负责人及原始证据见[执行清单](./README.md)；冻结契约未追加项目。

## 通过的主路径

B02并发跨书导航、B03动态插件设置、B04原生启动注册、B05真实Finder文件交付
开关、B06更新开关持久化与实际App消费者、B07固定版式模式、B09章节图谱补齐、
B11词典保存/列表/原生CSV导出、B15资料目录Reveal。各项只证明对应环境/主路径；
B09复用历史原生证据和受控模型，不证明内容语义质量。

## 失败与阻塞

| 项目 | 最终结论 | 影响与最小后续（本目标不继续执行） |
| --- | --- | --- |
| B01 | 缺语音端点；两次合成拒绝 | 配置隔离语音服务后补真实音频解码；列声音/退役已有证据 |
| B08 | 原生剪贴板读取权限拒绝，无法验证无损恢复 | 提供可安全备份恢复的剪贴板通道后补发布包快捷键；未覆盖原内容 |
| B10 | 隔离实例无模型配置，摘要生成未验 | 用隔离模型配置补一个生成场景；已有来源隔离/持久重读不当生成证据 |
| B12 | 检查已通过，无可用隔离升级版本 | 备好独立升级工件再验下载/安装；本次没有替换正式应用 |
| B13 | 连接取消通过，无隔离登录/删除测试账户 | 仅在测试账户补成功路径；未发送登录或删除请求 |
| B14 | 无测试账户和账单资格 | 补测试用量/账单交接；不执行付款 |
| B16 | 唯一复验仍漏指定笔记句号 | 文本忠实性仍有实际失败；保留样本，不以再次采样宣称修复 |

B16一次基线、一轮工具说明窄修复、一次复验后停止。复验完整范围及下划线/附着
已正确，但笔记标点未保留。主代理审阅两次完整答案、2/5次工具调用及最终状态，
同意失败分类；24项相关测试和Agent类型检查通过不能覆盖这个模型行为失败。
旧全量247/198/34/15统计不变；没有放宽断言或增加模型矩阵。

## 证据与清理审计

[243行索引审计](./coverage-audit.json)验证matrix/model/validation能力集合一致，
源码别名和证据文件存在。审计发现的LIB06第38/39引用错配已修正，第88阅读目标
索引已补齐。旧partial/待验不升格为通过；本报告不消灭历史其他平台/格式限制。

三名Astra medium子代理使用独立端口/资料，主代理独占物理桌面和共享文档。
primary恢复原两书、会话哈希、配置和15插件；9225自有实例恢复配置并停止。
root发布包保持原一书四标注，startup=false、fileAssociations=true、autoUpdate=true；
B05自有书及源blob释放，Finder恢复Playground，剪贴板未改写。独立测试服务和
9225/9226无监听；保留原有9224与release实例，不终止用户原有进程。

所有证据/文档修改按相关检查核对并本地提交；未push、未操作正式账户、未付款。
私有模型原输出未提交，证据只保留定位/哈希与必要摘要。

## 可发布性结论

本轮不足以支持“全能力已验收通过”的发布声明：B16已知失败仍在，六项指定
边界存在外部验证缺口。已通过的九项可以按证据范围宣称通过。其他OS、物理
跨设备、生产服务、全格式排列与新产品语义均不在本轮执行，不自动开启后续目标。
