# 有界并行验收目标

2026-09-13。用户要求替换旧全量goal，明确验收条件并允许多个子代理。
本文件是待启动的新目标契约；创建工具当前因旧goal未结束而拒绝创建，不能称已启动。

## 范围与完成定义

冻结起点main包含3244bc25、当前243行覆盖清单，复用截至第88流程的有效证据。
交付一次有明确结论的验收，不要求所有项目必绿，不穷举actor×格式×平台。
只补以下16项；已有部分覆盖行不自动追加组合。

| 编号 | 能力 | 一次主路径的通过条件 |
| --- | --- | --- |
| B01 | READ17 | 实际已编译语音provider列声音并合成可解码音频，核对回执与资源释放；缺远端配置记阻塞 |
| B02 | READ20 | 实际两书导航并发/替换一次，回执和最终打开书/位置一致，旧请求不覆盖新请求 |
| B03 | CFG09 | 实际插件非敏感动态设置可发现、授权读写重读；敏感字段不出现，停用后目录移除 |
| B04 | SET04 | 隔离应用启动项开关与原生状态一致，恢复原状态；无法隔离系统影响则记阻塞 |
| B05 | SET05 | 现有文件关联设置的宿主动作与原生结果一致并恢复；缺可用隔离边界记阻塞 |
| B06 | SET06 | 自动更新开关持久重读且现有控制器采用该值，恢复原值；不强求实际升级 |
| B07 | SET17 | 一份已有固定版式夹具切换阅读模式，真实阅读器状态/页内容正确，恢复设置 |
| B08 | SET69 | 发布包真实选区复制快捷键得到准确文本，恢复剪贴板；锁定则记环境阻塞 |
| B09 | MEM10 | 一章合成内容完成摘要/图谱补齐，实际宿主持久结果与来源一致；有效旧证据可复用 |
| B10 | MEM12 | 一个固定跨对话洞见/滚动摘要场景，核对实际来源及持久重读；模型质量单独评分 |
| B11 | EXT09 | 实际编译词典查询、收藏、列表及CSV导出一条自有词；复用第77/86已有步骤，仅补缺段 |
| B12 | SYS16 | 现有更新检查入口产生真实成功或诚实失败状态；下载/安装无可用隔离版本则列阻塞，不安装任意版本 |
| B13 | OPS06 | 现有账号流程一次合法取消/本地保留边界；真实登录/远端删除缺测试账户列阻塞，不使用正式账户 |
| B14 | OPS07 | 现有账号资格/用量及外部交接边界有证据；缺测试账户列阻塞，付款不在范围 |
| B15 | OPS10 | 设置展示真实隔离资料路径，Reveal打开正确目录；Actor定位控件证据单列 |
| B16 | 仅引文标注恢复 | 固定既有题库外问题一次基线；准确原文范围及笔记标点、不改CFI、不虚报完成；有针对性修复最多再验一次 |

通过必须有该边界实际动作、回执及重读；持久性要求按需补一次重启。
每项最终分类为通过、已复现失败、外部阻塞、未实现或不适用，附证据/环境/影响。
受控模型只证明调用链；真实模型与内存端口不替代Tauri绘制/原生保存。
第88目标修改以最新贡献正确且不跨书为通过条件，不要求清除历史消息里的旧目标。

## 并行和防发散

最多3个gpt-6-astra、medium thinking子代理；独占资料目录/端口，主代理独占
物理桌面与共享清单。建议分工：阅读与设置、记忆与模型、插件与系统；先确认
资源可隔离再分配，不能让多个代理操作同一应用状态。
完成子任务只领取上述剩项；不新建测试框架或新增产品能力。
每项最多一轮针对性修复和一次受影响复验；仍失败就保留结论继续，禁止采样直到绿。
同因环境失败两次即记录该项阻塞转下一项；不反复等解锁或重新请求凭据。
旧有效门禁复用，仅改动时跑相关检查。30分钟报告新增闭合项与剩余项数。

其他OS、物理跨设备、生产账户/付款、全格式排列、任意故障注入和UI美化均不执行；
在最终限制表保留，不成为本目标新任务。实际发现的其他问题也仅列后续。

## 结束条件

16项各有最终分类证据；243行已有证据索引核对完整且不夸大；临时数据/配置/进程
清理核对；相关改动检查完成并本地精确提交；交付一份通过/失败/阻塞/未实现/
不适用和可发布性结论。满足这些就结束，不自动扩大或开启下一目标。
验收报告完成不等于产品全部通过；仍失败的能力必须明确列出。
不推送，不触碰正式资料，不发送外部消息。
