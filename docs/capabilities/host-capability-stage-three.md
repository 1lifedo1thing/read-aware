# 第三段待验收清单

仅在用户指定的验收阶段，按本次能力和环境选择相关用例。此清单不阻塞已授权的
接线阶段，也不自动把所有故障、格式、平台或后续功能纳入当前 goal。
当前范围和停止规则见[宿主能力集中验收](host-capability-acceptance.md)；
未执行的用例仍保留为未验证，不能以修改清单或状态代替验收。

此清单只登记第一段已经接通、尚无第三段完整插件轮次证据的行为。
本文件是未完成验收的登记清单；部分路径已完成代表性桌面验收，具体以行级引用的验收记录为准。清单本身不是验收证据，不替代第二段行级
插件/流程映射；其他行随接通继续登记，不能把尚未登记的行视为通过。

## C06 类型化跨插件服务（Agent 链已接，集中验收尚未执行）

- Tauri加载Jumper0.10，在两书分别保存书签；Agent经call_plugin_service读取bookmark-page时只返回双方授权书的书签，外书和错索引文档不得出现。原插件侧调用方夹具已于2026-09-15退役。
- 验证all/book/current组合：当前书变化、撤权、停用提供者或替换服务后，在途结果失效；旧分页引用不得自动转给新实现。刷新重新发现后可恢复。
- 实际SQLite中书服务分页、旧游标、同ID外书拒绝和并发修改冲突；Worker启动/广播不收到提供者全局KV，book服务不能读取无归属的凭据/资产。
- 服务取消/超时/更新时已派发的持久写仍排空并保留实际结果，不能以拒绝回执声称回滚；重复链被拒绝，独立调用仍可继续。
- 原真实Bun双Worker检查随插件调用方夹具退役移除；受控存储检查不能替代以上Tauri/SQLite场景。Agent入口已接：集中验证真实对话发现服务、展示精确参数/权限后批准执行，拒绝无执行；书内越书、批准期间停用/更新、阅读上下文收紧应拒绝或取消。受控批准端口不替代此场景。

## C05 阅读操作条件（已接线，集中验收尚未执行）

- 真实Tauri中Agent全局/书内查询同一本书的朗读和模式条件：未开书、加载未就绪、无模式、无语音、空单元、未知执行状态分别显示。查询不启动朗读、网络或分段。
- 撤去reading:write或限定另一书时只返回权限条件；未打开目标书不泄露当前书/提供者。切书或关闭后旧页面刷新/动作仍绑定原bookId/sessionId并拒绝。
- 查询后停用语音/模式提供者，再点击开始/启用，执行明确拒绝；停止/关闭模式仍可恢复。重新具备条件后，实际音频开始/模式索引完成才构成成功。
- 受控控制器、实际React绑定和Bun Worker协议已过（原编译消费者夹具已退役）；以上真实桌面场景尚未运行，不能以本地检查代替。

## C05 推理操作条件（已接线，集中验收尚未执行）

- 真实Agent书内/全局查询图像推理条件：缺凭据、未选模型、
  不支持图像及本地配置完整分别返回正确条件，远端健康不因有目录或在线提示变成已验证。
- 无service:llm授权的Worker只收到权限条件；查询没有模型请求、凭据迁移，
  回执不含密钥、模型ID或端点地址。撤权/停用/取消不能交付迟到结果。
- 真实图片读取期间移除或切换模型配置，实际推理在派发前重验；ReadAware
  订阅使用同步会话认证。真实提供者的认证/模型/图像响应另作结果证据，不能由查询代替。
- 当前仅llm.infer已接；其他操作的对象范围、提供者和执行条件不计本节覆盖。

## C04 窗口与图片重排来源（集中验收候选，尚未执行）

- 已平移图片时，使用公开插件事件绑定上下文请求最大化、全屏和恢复，观察实际
  客户区尺寸、图片平移比例及反馈来源；分别核对请求后首个匹配帧和动画后续帧。
- 在窗口动作完成后手动拖动窗口尺寸，确认这是可独立触发的新操作；旧窗口样本
  不能被新尺寸沿用。图片变换或关闭发生在尺寸读期间时，迟到样本不能改写新视图。
- 无关父组件重绘与重复ResizeObserver通知不产生新图片变化。相同viewport内
  字体/工具栏等引起的容器变化、原生读取失败及恢复，须单独补齐来源后验证。
- 同时覆盖流式/固定版式阅读器重排与窄窗口面板分支。受控原生和JSDOM只证明
  协议与状态时序，不能替代这些实际布局、动画和用户输入证据。
- 外层阅读器viewport已接匹配窗口尺寸的选区清除和正文宽度来源。真实桌面须验证
  窗口动作后的文本测量；查询在途时新选区、相同选区上的新输入、换章、引擎替换
  和卸载不能被旧反馈覆盖。Foliate内部ResizeObserver及面板响应式来源仍待接线。

## SYS03 外部设置与更新隔离

- 正式插件升级/首次schema迁移与设置表单、Agent及获授权插件的设置批次并发：已受理保存先落盘再取基线，迁移中保存明确busy且批次不部分写，其他插件和普通设置仍可用。
- 在候选健康检查、迁移、贡献提升、清理、原生恢复和旧实例重启处注入失败；范围内不得接受外部设置，清理未完成不得恢复。旧窗口/迁移期间打开的设置表单重试显示过期，关闭重开后才可保存；检查错误提示与焦点/键盘操作。
- 同步pull在迁移期间抵达，恢复/完成后重新读取最新偏好投影，物理KV写结束前下一次迁移不得开始；含删除、多插件和读取/持久失败，不只检查乐观镜像。
- 已接迁移耐久决定：候选多次改值/删除/临时键只在原生accepted事务以最终净差异进入domain_events；事件失败应拒绝更新并恢复旧文件/数据。准备或接受回执丢失按ID查记录，不能重复发布或误回滚；接受后普通保存失败仍走既有重试。清理/恢复失败隔离目标，其他插件可回填。
- 原生子进程/WAL恢复已定向验证；集中在真实Tauri对prepared、文件切换空窗、迁移后、accepted后终止进程再启动，核对文件版本、KV/docs/schema与日志同属一个决定；含首次安装、启动schema迁移、坏备份、webview重载只查状态、隔离后的设置/卸载/更新拒绝。真实packaged/其他平台证据仍缺，完整备份引擎尚归OPS08，不能宣称整个SYS03完成。

- 现有v1备份与升级/启动迁移并发：数据操作前busy拒绝，备份期间新插件更新/外部插件设置不进入，既有保存先落盘，同步等待后重读；导入使旧表单失效，失败导出要等全部读取结束。实际取消在等待写入阶段不启动合并，已开始合并仍返回真实结果；原生选择/保存期间无锁。检查隔离插件未在目录加载时同样拒绝备份。该用例不证明v1成为完整备份。

## LIB06 导入任务

- 正式插件组合应采用library1.20（原第一方消费者夹具已于2026-09-15退役，当前无插件消费者，本条保留为公共路径场景）：资源选择/封口→startImport→观察或有限等待→最终receipt→按实际书籍继续操作。覆盖字节输入、已有资源、同名不同内容、同内容不同名、修复缺失原件、坏书/加密/不支持文件；queued或staging不能当成功。
- 全局Agent真实对话保留每次批准，检查工具启动只给queued，再用get_book_import_tasks等待实际终态；最终导入/duplicate/失败与SQLite书籍、事件、原文件一致。书内工具不出现；其他会话/插件读取和取消旧taskId拒绝。
- 在资源读取/格式准备、首次blob或stage派发前、原生stage中、事件提交中分别取消；前置取消不写，已受理结果不被取消遮蔽。关闭/停用/升级及Worker崩溃时确认资源租约、已受理字节任务排空，重开不复活旧句柄或重复写。
- 任务池每owner2个、全App4个物理执行，取消不提前退槽；这不是picker/旧直接导入的全局额度。64句柄终态淘汰、16观察配额、慢观察合并、观察取消及0..30秒等待超时都检查真实状态和回调释放。超过字节/传输上限失败明确，可改用授权资源；不以全量读内存冒充流式进度。
- packaged/跨平台真实导入与磁盘故障、超时/进程中断后复查书架：句柄不耐久恢复，未知不能声称未落库，不能盲目自动重试或取消后删书。阶段进度不提供原生stage内的虚构字节百分比。

## SET18–21 阅读 AI 动作

四个实现关闭条件已经满足，保持接通（待 E2E）；第二段安排正式插件
通过现有 settings 路径组合工作流，不为切开关再建四套插件工具。

- 分别启用解释、释义、翻译和章节总结，真实选区 More 与命令面板点击
  进入本书聊天，检查保存的实际意图/附件、一次发送和模型最终答案。
  关闭 askConversation 仍独立可用；关闭各自开关后两 UI 和下一次模型
  请求都不包含对应动作，旧工具调用也拒绝。设置恢复、重启同样检查。
- 双 scope 自主 Agent 对话：全局进入当前书并只报告 started，不伪造
  已完成答案；书内在当前回合回答，不向自身递归发送或操作其他书。
  章节请求应实际读取当前完整提取章节，不拿可见页面或其他章节代替。
  留实际工具、模型输入/输出和语义审查，包含各格式、多 href 章节和剧透限制。
- 超12000的规范化预览须还原完整版本化范围；32000边界、字符对、PDF
  唯一 quote、缺范围/超限/重复 quote/版本更新不得静默解释前缀。
  八语翻译意图和长标签、键盘操作、窄窗菜单不溢出，真实用户选择不被
  菜单/聊天焦点变化错误替换。句段合成菜单无活动选区时不能解释旧选区。
- loading→ready、忙、超时、读失败、切书/关书/换版本、选择更新、调用者
  取消、命令面板退出/重开及并发点击都核对实际发送次数。等待中关闭又
  重开开关不能复活旧动作；已经 send 接受的回执不冒充远端调用已撤销。
- 插件有/无精确路径授权、撤权/退休、持久写失败回滚时，真实 UI、工具
  发现和调用资格一致；两文本隐私设置、localOnly 在准备期和推理期变化
  分别核对，禁止的正文不因特定动作泄露。已发送后的停止/错误/重试沿用
  正常聊天，保存失败不能算成功持久化。
- packaged 正式插件安装/CSP、SQLite 会话持久、真实模型错误/重试并入
  对应插件完整轮次证据。当前只有定向服务/实际端口/脚本 Agent 和 JSDOM
  证明，不包含这些真实轮次结果；本组没有启动桌面或正式插件构建。

## MEM08 已接通的显式实体注册表链

- 正式 Worker 插件读取 identities/members/aliases，包含原成员晚到定义、
  同名但不同身份、pending keeper、未知 ID、分页中远端变更，不能混页或伪造身份。
- 真 Agent 对话 query_entities → manage_entity → 聊天中精确候选确认 →
  原生事件/投影/outbox。批准、拒绝、等待时取消、批准后远端并发变更分别验证；
  关闭自动记忆构建时显式管理仍可用，不自动导入其他书的 digest 人物。
- 合并确认显示同一版本的保留根名称/ID和成员数量；保留原定义与别名，
  修改已合并原成员不冒充修改 keeper。重放、重启、checkpoint bootstrap
  和跨设备顺序合并后再次查询，明确本机条件事务不等于分布式 CAS。
- Worker read/write 授权、撤权/升级退休、IPC 前取消、IPC 后原生成功/失败，
  以及 RPC 期限/Worker 崩溃后未知写结果，必须核对真实事件而非假定回滚。
  只有实际 changed 成功广播，冲突和 no-op 不新增事件。
- 当前只有定向原生测试及进程内模型/IPC/Worker 替身，不是本节通过证据。
  有界自动巩固、记忆正文分批续跑和完整注册表分区扫描已接通，按下一节验收。

## MEM08 自动巩固与派生画像检查

- schema39/40 大正文跨多次空闲轮次/重启续跑，只对未完成节点推理；中间页不能
  出现在画像或同步事件中。末次完整发布才原子清页并结清，来源变更拒绝旧页。
  逐节点压缩后保留精确游标/归并状态，实际越过4096累计节点再重启继续；
  压缩落库失败不能丢源或重跑模型，原生成功但回执丢失后应安全恢复，旧写入不覆盖新进度。
  关闭自动构建时已派发页写入/压缩排空，重新启用复用真实已保存页；完整插件检视
  仍走 profileContext。真实模型是否保留矛盾及实体依据须审查，脚本输出不能证明。
- 超大注册表包含跨页根、合并原成员、累计大别名集合及同名不同人；记录完整
  分区输入和续跑游标。筛选后读取原始成员/根定义，不能把摘要当原始身份数据，
  新生成ID与未入选的现有ID碰撞必须拒绝。有待处理引用被容量限制舍弃时，
  hasMore强制partial；用真实模型核验后续轮次是否推进剩余工作，不能只检查结构。
- 真实空闲维护读取合格 user/global 记忆并调用 fast 模型，保存派生画像、
  实体事件及来源。核验同名不同人、相反证据、虚构人物和不确定性；结构校验
  不替代语义审查，保存实际输入/输出及人工判断。普通维护无工作仍应检查画像。
- 原生成功后重启，不重复处理未变版本；远端证据新增、失活、改写、同内容新事件、
  checkpoint bootstrap/backfill、人工画像或实体改变分别核对失效与后续维护。
- 模型截断、非法输出、超预算、partial、SQLite 故障和推理期间并发更新，不能
  错误结清。关闭自动构建取消读/推理并阻止新写，已派发事务排空到真实结果。
- 正式 memory 2.3 Worker 插件的 profileContext 三类分页/观察及双线程
  inspect_user_profile 同源；人工摘要编辑仍独立。超100来源、跨事件证据续页、
  UTF-16字符对、旧版本冲突、无/空/坏/失效派生块都要检查，不泄露原始traits或正文。
- 遗忘或新增合格证据后，下一轮聊天立即排除旧推断，但显式检查仍标 stale
  并显示历史来源；不能把 current 状态当语义正确或自动访谈完成。读权、撤权、
  退休中原生迟到、Worker取消、读失败/观察恢复分别验证，无自动写入或推理。
- 当前只有定向替身和原生事务证据。本节没有真实插件轮次通过记录，不启动桌面。

## MEM13 双端 context bundle 公开链

第一段已接 memory 2.4 `queries.context.history/read/export`、
`commands.context.capture`、Agent 四个工具与设置→数据的 Context bundles 组，
行为接通（待 E2E）。第二段须为本行指定正式插件流程，第三段该插件整轮须检查：

- 原生入口：真实书库/全局线程出现在 scope 选择器；捕获后 toast 与版本列表
  一致，再次捕获报未变化且不新增版本；点击保存弹出真实系统对话框，取消不
  写文件也无成功提示，确认后文件字节与 read 返回的 artifact 逐字节一致
  （尾随换行）；保存期间修改来源或阅读位置后再次保存必须失败并释放句柄；
  关闭设置页释放全部句柄；非桌面构建只显示说明。

- 四 recipe 各自的域授权：只有 memory:read 的插件对 book_memory_context /
  conversation_insights_context / book scope 的 history、read、export 必须
  收到 memory/forbidden；capture 无 memory:write 拒绝；撤权/升级后旧激活
  与新激活分别核对，退休中迟到结果不交付。Worker 真实 RPC 走嵌套
  `domains.memory.*.context.*` 路径与末位 options 槽，取消在 capture 派发前
  阻止发布、派发后排空真实回执。
- 真实 SQLite 归档：多次 capture 后 history 分页、cbhist1 续页冲突、
  重放/checkpoint bootstrap 后同一版本可读且 hash 一致；read 对未保留版本
  返回 null，损坏行报错不当空。
- 书内 recipe 的旧版本披露：在第 k 章捕获后倒退到更早章节，read/export
  必须 memory/forbidden；前进后可读；标记读完后所有版本可读；换版本（重新
  导入同书不同文件）后旧版本以 edition 拒绝；解除读完标记回到围栏后拒绝
  曾经 all 的版本。核对错误码而非空内容，不得出现被裁剪的 artifact。
- export 句柄：插件 services.resources.stat/read/save 可用；导出后修改任一
  来源表（标注、记忆、画像、纪要、私有文档、blob）或改变该书阅读位置后，
  read/save 必须失败并释放；已经确认的保存文件保留；退休释放全部句柄；
  Agent 的 read_resource_text 对 context 句柄拒绝而 save_resource 可用。
- Agent 双 scope：书内对话 bookId 越书拒绝、默认当前书；全局对话
  book_memory_context 缺 bookId 拒绝、conversation 默认本线程；模型实际
  收到的 artifact 与归档 read 逐字节一致；工具描述不诱导模型自行选 scope。
- 上述结果与真实模型输入/输出、原生日志和 SQLite 快照一并归档；当前仅有
  定向 TS 测试、进程内 Worker 与受控 IPC，不启动桌面或正式插件构建。

## SYS17 关闭/退出保存协调

第一段已接 ShutdownCoordinator、原生 onCloseRequested 拦截与 Rust ExitRequested 一次
延迟；close/quit 对 Agent/插件仍不开放。第三段真实 Tauri 须检查：

- 阅读中点击红色关闭按钮：窗口先不关闭，SQLite 中该小时桶的阅读时间与位置
  已落库后窗口才消失；重启后进度/时间一致。插件（RSS/Jumper）在关闭前排队的
  文档/KV 写在重启后可见；日志有各 owner 的 flushed/failed/timed-out 回执。
- 保存API凭据和漫游偏好后立即退出：分别延迟 secret_set、设备身份查询、commit_events，
  确认关闭等待整条链；重启核对本机值及对应漫游事件。凭据拒写显示/记录失败，
  其他成功偏好的事件仍排空；超时可关闭但不能伪称写入成功。
- Cmd+Q / 应用菜单退出：进程不立即结束，webview 冲刷后 app_exit_confirm 退出；
  注入 webview 无响应时 10 秒兜底退出；退出后无孤儿 Worker/进程。
- 冲刷中再次点击关闭或重复 Cmd+Q 不触发第二次冲刷；SQLite 拒写时仍在期限内
  关闭且回执 degraded，日志不含用户内容。Windows/Linux 的关闭与 Alt+F4/SIGTERM
  路径分别实测，不从 macOS 外推。

## MEM06 画像摘要及已接通的编辑/备份链

memory 2 的摘要链已接通；MEM07 完整访谈/原子播种已接 memory2.5，
见 onboarding.md 的隔离 Tauri 证据，正式模型/插件轮次仍待验；MEM08
可恢复大输入巩固仍有实现缺口，不能用本节替代其关闭条件。第三段实际插件整轮必须检查：

- 真实旧库升级：有旧 KV、空摘要、已有 summary/null 事件、name-only
  事件及大于 16000 UTF-16 的历史摘要，核验 SQLite 日志/outbox/投影与
  旧键退休。坏迁移/未完成 backfill 不得展示假空画像或覆盖明确清除。
- 同一摘要经插件分页/观察、双 scope Agent 查询及下一轮真实提示词读取；
  保留同章会话。不得以直接调用工具代替自主对话中的实际批准流程。
- 两个进程/设备的并发更新、A-B-A、取消、撤权/退休、已派发 SQLite
  写失败与观察错误恢复。各自离线成功不是跨设备 CAS；核验 HLC 合并
  后所有设备的最终摘要与重放一致，checkpoint 新设备 bootstrap 也一致。
- v1 真文件导出/导入：只恢复摘要，不冒充完整画像/实体备份；空键值、
  缺键、长旧文本、导入中途画像变动和部分失败均保留实际回执/错误。
- 正式安装 memory 2 消费插件；memory 1.x 要明确不兼容，读权不能写，
  Worker 无内部 initialize/restore/raw KV 权限。批准文字要说明同步影响。

当前仅有定向 Rust 事务和受控 IPC/权限/消费者测试、类型检查与插件
构建；没有本节真实轮次证据。

## OPS10 数据目录显示与 Reveal

代码入口：`maintenance 1.4 openSettings("data-location")` 与双 scope
`open_maintenance_settings`，宿主 `DataLocationGroup` / `nativeDataLocation`。

第三段必须检查：

- 从第二段覆盖 OPS10 的正式安装插件进入目录设置，确认显示的是隔离
  Tauri 数据目录，与该应用实际 SQLite/Blob 根目录相同，不是正式用户库。
- 插件和 Agent 返回值只有 `opened` 与 `surface`，不含目录、用户名、文件
  内容；定位时系统文件管理器不打开，必须由用户点击 Reveal。
- 点击 Reveal 后系统文件管理器确实显示对应目录；连续点击不重复派发
  同一在途 UI 操作。原生打开失败需捕获日志、稳定错误和本地化反馈。
- 目录查询故障不显示空目录或旧路径；瞬态错误重试恢复，权限错误不
  提供虚假重试。页面卸载、调用者退休/取消期间不交付迟到状态或定位。
- 在 packaged 轮次中通过正式插件安装重新验证该入口。截图/日志/IPC
  回执归入对应插件同一轮 evidence；macOS 的系统文件管理器行为不能
  外推为 Windows/Linux 已验。

目前证明仅为类型检查及针对性测试：调用了实际 Tauri JS API，但原生
IPC 为受控适配器，UI 是 JSDOM，未操作真实文件管理器。第二段尚未
指定正式插件流程，第三段结果与 evidence 路径仍为空，OPS10 保持
“接通（待 E2E）”。

## SET04 开机启动

阶段一只通过纯序列化/原生提交顺序、受控 IPC、权限和挂载组件测试。
第二段为本行指定正式插件工作流，第三段该插件整轮必须包括：

- 记录隔离 app identifier、可执行路径和数据目录；启用前后确认生产
  ReadAware 的启动项未改变。普通及带空格/特殊字符的 packaged 路径均检查。
- 原生设置开启、Agent 关闭、授权插件开启：核对 OS 注册与 SQLite，
  重启应用仍一致；真实退出登录/登录证明开启会启动、关闭不会启动。
- Windows 核对 HKCU Run 和 StartupApproved；Linux 核对 XDG autostart、
  Exec 参数与桌面环境；macOS 核对 LaunchAgent plist 和登录限制。
  系统策略拦截不得拿“文件存在”冒充实际登录成功。
- 外部关闭启动项后修改语言/启动视图，不得重新开启；焦点返回刷新状态。
  原存储值与 OS 不一致时，关闭不能因 KV 无变化而成为空操作。
  安装路径移动或系统加入额外元数据后，已有启动项仍应可识别和关闭。
- 注入 OS 读取失败、写入部分失败、核验不一致、SQLite 批量事务失败、
  补偿失败；确认无虚假成功，原始信息只进日志，恢复后 UI/两端同源。
- 并发 UI/Agent/插件设置、队列中取消/撤权、卸载后迟到状态、双击、
  批量恢复/前缀替换/清空数据均验证；已派发 OS 操作不假称可撤销。
- 无路径权限的插件不可读写；包内 Worker 不得调用原始 autostart IPC；
  packaged CSP 与安装轮次保留对应证据。
- 收尾关闭隔离启动项并核对移除，保留生产启动项原始状态。测试证据
  必须记录平台/版本/路径/操作/实际结果，不能只贴 mock 测试通过数。

当前无上述真实轮次证据，SET04 保持“接通（待 E2E）”。

## SET05 文件关联与系统文件接收

三平台第一段接线已完成，SET05 接通（待 E2E）。Windows 的 HKCU/NSIS、
Linux 的用户 desktop/MIME/xdg 工具与 macOS 接收门控均已实现；定向测试
不代表下面的真实安装轮次已经执行。正式插件整轮须保留：

- macOS Info.plist 声明的 14 个扩展与 RunEvent::Opened 冷/热启动；
  关闭后收到文件不导入、不导航，运行中拒绝的文件不触发宿主 show/focus。
  关联声明仍保留，OS 仍可能启动/激活应用，冷启动仍会创建应用窗口。
- Windows 注册表、Linux xdg 的真实注册/注销与处理器列表；默认应用
  和其他软件条目保持，安装/升级/卸载及独立 identifier 不污染生产。
- Windows NSIS 首次启动注册、更新后仍按持久开关、移动安装目录后的
  命令刷新、14 个 OpenWithProgids 的移除、卸载 ACL 拒绝不继续删除
  程序文件；旧 NSIS 通用类按命令/图标/标签确认归属再迁移，恢复备份
  不能覆盖后来选择的新默认。机器级旧关联需安装器迁移，不得假报注销。
- Linux 在实际桌面环境核对同一 productName.desktop 的系统/用户优先级、
  MIME Cache 与处理器列表，禁用后的旧 Added/default ReadAware 不能绕过。
  保留其他应用的默认顺序与登录 scheme，DEB/RPM 依赖实际安装，AppImage
  路径不能指向临时挂载；移动/升级后不重复注册。卸载后检查 TryExec 对
  遗留用户覆盖的过滤。第三方 AppImage 集成器创建的不同 ID 没有本应用
  owner，不能擅自删除或声称已清理，文件接收仍须被同一开关拒绝。
- Linux 注入 xdg 工具缺失、非零退出、超时/子进程、文件权限与 SQLite
  故障，核对补偿后两类缓存和原始文件；外部编辑冲突不得覆盖，失败
  必须有日志和本地化状态。临时 XDG 命令单元测试不替代桌面管理器行为。
- UI、Agent、授权插件切换的持久化结果与 OS 行为一致；开启后新文件
  正常导入，关闭期间的文件和关闭前旧批次在重开后不复活。
- 在原生排队、读取文件信息、前端检查后到原生准入前、等待导入完成
  四个时间点关闭：前三个不再准入导入；已经原生准入的导入允许完成，
  导入后复核必须抑制迟到导航请求，不声称回滚已派发的导入或导航。
- 原生读取/注册/SQLite 提交失败、失败后重试、并发事件与撤权、冷启动
  首次挂载、清空数据与前缀恢复。保留原生日志、数据库、UI 和系统结果。
- 手动导入、拖放、readaware:// 登录不受本开关影响；packaged CSP 与
  正式插件安装仍需实测，不以受控 IPC/内存 SQLite 代替。


## C05 正文准备条件与当前书任务（待集中执行）

- 在真实 Tauri 通过双 scope Agent 查询 library.text.prepare，确认只查条件不会抽取/下载；本地有源、缺文件且同步关闭、缺凭据、虚拟书提供者缺失分别报告对应状态。
- 真实 Worker 查询后关闭/更换书源与同步传输，再执行准备，确认实际重验，不将旧 available/unknown 当成功；正常下载与解析必须以真实正文状态/SQLite为证据。
- 仅当前书授权的插件准备正文：初始读取/历史写等待、运行及暂停期间切书，取消该租约且不能交付旧书结果；保持其他消费者，已派发I/O排空，终态释放围栏。暂停/恢复/优先级/取消通过公共命令执行。
- 插件重建条件→确认表单→真实重建，确认拒绝不启动、成功以最终索引为准；旧查询不预留任务容量。原消费者夹具已退役，需新的插件消费者或 Agent 路径执行；此处仅登记场景，当前批次未执行。

## C07 原子语义事务集中验收

- 实际Agent对话预览本书元数据+本书排版，核对冻结批准内容；拒绝无写，批准后事件/设置一致落盘；书内全局设置或越书拒绝。批准期间改变目录/字体提供者后不得应用旧条件。
- 正式插件通过transactions1.0组合元数据、设置、私有文档，观察同一原生事务全成全败及同源回调；document.check不通知假写。仅当前书授权切书、停用后旧预览不可提交，已派发写先查回执。
- 提交后退出重启，以原owner/id查回执并previewUndo；无后续修改时以新事件及条件写恢复，修改过的对象/排版继承基线拒绝覆盖；插件卸载清私有数据后旧回执不残留。不同owner拒绝查到回执。
- 安装态Workspace Profiles0.8应用预设和可见撤销；预设文档在预览后被修改则设置全不写；全局与本书覆盖的恢复、字体实际显示分别观察。受控批准、Bun Worker拒权和SQLite单测不能替代这些场景。
