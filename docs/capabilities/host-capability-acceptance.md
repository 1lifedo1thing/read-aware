# 宿主能力集中验收

更新时间：2026-09-15。实现基线：`17860738`。

这份文件是当前能力工作的唯一执行清单。它只描述尚未完成的交付边界，不记录逐
commit 过程。能力现状以 `host-capability-matrix.data.ts` 和运行时代码为准，详细
历史与原始运行工件保留在 Git 历史中。

## 当前判断

- 当前矩阵 243 行，其中 173 行的 Agent 与插件均已接通或处于“接通（待 E2E）”。
- 其余行混合自动行为、内部实现、刻意不开放、非桌面能力、未来能力、局部语义和
  验收缺口，不能作为待实现功能直接求和。
- 当前桌面公共范围没有“宿主、Agent、插件三端都完全缺失且必须补建”的能力。
- Jumper 所需的目录、精确搜索、导航、历史和书签主链已经实现；后续能力工作不再
  以“Jumper 仍不可实现”为前提。
- C01–C09 进入集中验收阶段。除非下面某个场景复现了具体断点，否则不再增加公共
  API、观察来源、操作类型、通用任务或事务基础设施。

## 剩余交付

| ID | 验收对象 | 一次集中场景的完成条件 |
| --- | --- | --- |
| C01 | 按书记忆、图谱、上下文归档 | 受限插件查询/分页/观察/条件修改并运行图谱任务；切书或撤权后旧结果和资源失效；真实 Tauri/Worker/SQLite 回执一致 |
| C02 | 按书会话 | 查询本书转录、摘要和运行状态，提出一轮对话，停止或批准清空；跨书数据不出现，已派发与未派发取消边界如实返回 |
| C03 | 受限宿主命令和工作区 | 发现工作区与命令、执行允许的导航；跨书选择、集合和关闭其他书的操作被拒绝；真实 UI 状态与回执一致 |
| C04 | 跨插件因果防环 | 一个真实插件观察变化后通过绑定上下文写回，同一规则循环被拒绝，独立用户操作和合法后续任务继续；覆盖 Worker 回调及一个阅读器/原生反馈链 |
| C05 | 操作可用性 | 现有 23 种公开 operation 保持契约覆盖；按阅读对象、模型、账号/提供者、OS/UI、维护流程五类各跑一次查询→状态变化→执行重验，不为未登记的“其他操作”扩表 |
| C06 | 类型化跨插件调用 | Jumper 调用 Text Desk 书签服务，验证契约版本、双边权限、书域、取消/停用；Agent 路径必须经过真实批准 |
| C07 | 跨领域事务与撤销 | 设置、私有文档和领域事件三域提交全成全败；冲突不写，重启后能读回执并按条件撤销，后续修改不被覆盖 |
| C08 | 耐久任务恢复 | 正文或图谱真实任务可暂停、取消、恢复；杀进程/重启后从检查点继续或进入 needs-attention，不盲重放未知外部结果 |
| C09 | 变化日志 | 插件和 Agent 按授权打开游标、分页读取、重启续读；跨书/私有数据不泄露，备份恢复、wipe 或过期游标明确失效 |

每项最终只能标为：通过、已复现失败、外部阻塞或不适用。失败可以触发有范围的
修复和一次复验，但不能重新开启全仓能力扫描。

## 明确停止规则

1. `partial` 只是调查入口，不自动加入本清单。
2. C04 的“其他来源/其他消费者”和 C05 的“其他操作”不是有效待办；新增对象必须
   先指出上述九个场景中无法完成的具体动作。
3. 单元测试、类型检查、文档生成和 commit 数仅作支持，不计作验收完成。
4. 已有接线不重复实现；集中验收只覆盖本轮新增或仍无真实证据的边界。
5. Windows/Linux、任意格式排列、生产账号/付款、第三方服务质量和未来产品能力不
   属于本轮范围；具体安全或正确性失败除外。

## 相关文档

- 结构化现状：`host-capability-matrix.data.ts`
- 责任和边界：`host-capability-model.data.ts`
- 当前交接：`host-capability-delivery.md`
- 已规划的真实桌面场景：`host-capability-stage-three.md`
